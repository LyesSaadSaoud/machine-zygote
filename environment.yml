name: machine-zygote
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=1.24
  - scipy>=1.10
  - matplotlib>=3.7
  - pip
  - pip:
      - pytest>=7.0   # optional; tests/run_tests.py needs nothing extra
